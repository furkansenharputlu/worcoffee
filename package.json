{
  "name": "worcoffee",
  "version": "0.1.0",
  "private": true,
  "description": "A community-curated map of coffee shops & spots that are great for remote work.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "watch": "node scripts/watch.mjs",
    "dev": "node scripts/watch.mjs & npx --yes serve dist -l 3000"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
